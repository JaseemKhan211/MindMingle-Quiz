extends base

block content
    main.result-page
        .result-card
            h2.result-title 🎯 Quiz Result

            .progress-wrapper
                .progress-circle(style=`--percent:${result.percentage}`)
                    span #{result.percentage}%

            h3.score-text
                | Score:
                strong #{result.score} / #{result.totalQuestions}

            if result.percentage >= 80
                span.badge.badge--excellent 🌟 Excellent
                p.result-msg Outstanding performance. Keep it up!
            else if result.percentage >= 50
                span.badge.badge--pass ✅ Passed
                p.result-msg Good job. You passed the quiz.
            else
                span.badge.badge--fail ❌ Failed
                p.result-msg Don’t worry. Practice and try again.

            .result-actions
                a.btn--btn.btn-----primary(href='/') Go Home
                a.btn--btn.btn-----secondary(href='/quiz') Retry Quiz